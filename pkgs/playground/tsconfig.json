{
    "extends": ["../../shared/tsconfig.base.json"],
    "include": ["src", "./package.json", "*.ts"],
    "references": [{ "path": "../gameboard" }],
    "compilerOptions": {
        // required to typecheck `bun-solid-plugin` import in `build.ts`
        "customConditions": ["bun"],
        "paths": {
            "#src/*": ["./src/*"]
        }
    }
}
