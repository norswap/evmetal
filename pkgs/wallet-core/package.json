{
    "name": "@evmetal/wallet-core",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "imports": {
        "#src/*": "./src/*"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "make clean && make setup && bun build.ts all",
        "lint": "biome check",
        "lintfix": "bunx sort-package-json && biome check --fix",
        "make": "make",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@norswap/utils": "workspace:",
        "viem": "^2.47.6"
    },
    "devDependencies": {
        "@bunup/dts": "catalog:",
        "@types/bun": "catalog:",
        "typescript": "catalog:"
    }
}
