{
    "name": "@evmetal/wallet-core",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "imports": {
        "#src/*": "./src/*"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "make clean && bun build.ts all",
        "lint": "bunx biome check",
        "lintfix": "bunx sort-package-json && bunx biome check --fix --unsafe",
        "make": "make",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@norswap/utils": "workspace:",
        "viem": "^2.51.2"
    },
    "devDependencies": {
        "@bunup/dts": "catalog:",
        "@types/bun": "catalog:",
        "typescript": "catalog:"
    }
}
